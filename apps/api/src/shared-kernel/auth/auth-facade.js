'use strict';

const {
  createAuthService,
  listSupportedRoutePermissionCodes,
  listSupportedRoutePermissionScopes
} = require('./create-auth-service');

module.exports = {
  createAuthService,
  listSupportedRoutePermissionCodes,
  listSupportedRoutePermissionScopes
};
